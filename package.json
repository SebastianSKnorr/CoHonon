{
  "name": "cohonon",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "npx serve . -l ${PORT:-3000} --no-clipboard",
    "translate": "node scripts/translate.mjs"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.36.3"
  }
}
